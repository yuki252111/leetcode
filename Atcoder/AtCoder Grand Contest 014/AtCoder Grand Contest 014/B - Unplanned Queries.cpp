/*#include <iostream>
#include <vector>
using namespace std;

int main() {
	int N, M;
	cin >> N >> M;
	vector< vector<int> > ms(M, vector<int>(2, -1));
	for (int i = 0; i < M; i++) {
		int start, end;
		cin >> start >> end;
		ms[i][0] = start - 1;
		ms[i][1] = end - 1;
	}
	int total = N * N;

}*/